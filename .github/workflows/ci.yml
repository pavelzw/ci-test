on: 
  push:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix: 
        os:
          - macos-10.15
          - macos-11
          - macos-latest
          - macos-12
    steps: 
      - uses: actions/checkout@v2
      - run: sudo /usr/bin/pip3 install ansible
